// The root server.js file is the Node.js entry point required by the project instructions.
// It loads the backend server module so this project can later be launched from Electron
// without changing the application structure.
require("./src/backend/server");
